name: med
description: "MED — Optimisation des temps de trajet dans le réseau de transport parisien (EFREI, Solution Delivery 2025-2026)."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # TODO(V1): décommenter quand le pipeline data aura généré le graphe
  # assets:
  #   - assets/graph/idfm_graph.json
